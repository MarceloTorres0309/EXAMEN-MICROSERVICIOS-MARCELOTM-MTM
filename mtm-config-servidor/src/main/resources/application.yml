server:
  port: 7070

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/keinraf/Microservicios-KEVINRAF-MANUAL.git
          searchPaths: config-data
          default-label: main
  security:
    user:
      name: marcelotorres
      password: marcelo0309
